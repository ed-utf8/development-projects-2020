package eu.kiieranngd.advance.Commands.command.guildadmin;

public class SettingsCommand /*implements Command*/{
    /*@Override
    public void dispatch(User author, TextChannel channel, Message message, String content, MessageReceivedEvent event) throws Exception {
        EmbedBuilder eb = new EmbedBuilder();
        Guild guild = event.getGuild();

        if (!event.getMember().hasPermission(Permission.MANAGE_SERVER)) {
            channel.sendMessage("You don't have permission for this Command.");
        } else if (content.isEmpty()) {
            eb.setAuthor("Guild Settings for " + guild.getName(), "https://advance.xyz", guild.getIconUrl())
                    .addField().build();
        } switch (content) {
            case "prefix":
            case "modlogchannel":
            case "":

        }
    }

    @Override
    public String[] getAliases() {
        return new String[0];
    }

    @Override
    public String getName() {
        return null;
    }

    @Override
    public String getDescription() {
        return null;
    }

    @Override
    public String getHelp() {
        return null;
    }

    @Override
    public Category getCategory() {
        return null;
    }*/
}
